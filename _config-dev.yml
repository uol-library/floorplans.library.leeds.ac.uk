# Development overrides for https://dev-floorplans.library.leeds.ac.uk (served by
# Apache from public-dev/). Layered over the base config in _config.yml:
#
# JEKYLL_ENV=development bundle exec jekyll build --config _config.yml,_config-dev.yml
#
# The development environment enables debug logging and the noindex robots tag
environment: development
url: "https://dev-floorplans.library.leeds.ac.uk"
baseurl: ""
destination: public-dev/
